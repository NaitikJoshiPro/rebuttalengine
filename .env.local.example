# Neon Postgres — use pooled URL for Prisma queries, direct for migrations
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/rebuttalengine?sslmode=require&pgbouncer=true&connect_timeout=10"
DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx.neon.tech/rebuttalengine?sslmode=require"

# NextAuth
NEXTAUTH_SECRET=""   # generate: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"

# Vercel Blob
BLOB_READ_WRITE_TOKEN=""

# Anthropic (optional — enables AI rebuttal enhancement)
ANTHROPIC_API_KEY=""
